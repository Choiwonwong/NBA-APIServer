apiVersion: apps/v1
kind: Deployment
metadata:
  name: quest-api
  namespace: quest
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: quest-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: quest-api
    spec:
      containers:
        - name: quest-api-ctn
          image: 622164100401.dkr.ecr.ap-northeast-1.amazonaws.com/quest-api:0.0.0
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - secretRef:
                name: quest-rds-credentials-secret
            - secretRef:
                name: quest-eks-url
            - secretRef:
                name: quest-api-sa
          # env:
          #   - name: WEB_URL
          #     valueFrom:
          #       secretKeyRef:
          #         name: cors-origin
          #         key: WEB_URL
          volumeMounts:
            - name: quest-api-ca
              mountPath: "/root/.kube/"
              readOnly: true
      volumes:
        - name: quest-api-ca
          secret:
            secretName: quest-api-sa
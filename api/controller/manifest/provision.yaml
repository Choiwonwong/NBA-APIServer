apiVersion: v1
kind: Pod
metadata:
  name: full-eks
spec:
  containers:
  - name: full-eks-ctn
    image: kimdoran/ubuntu-terraform:5.0
    command: ["/bin/sh", "-c"]
    args: ["/provision/1013test.sh"]
    envFrom:
    - secretRef:
        name: aws-credentials
  restartPolicy: Never